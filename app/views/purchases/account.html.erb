
<h3>お届け先</h3>

<%= form_for(@purchases) do |f| %>

	<h4>郵便番号</h4>
	<%= f.text_field :add_postcode %>

	<h4>住所</h4>
	<%= f.text_field :add_address %>

	<h4>電話番号</h4>
	<%= f.text_field :add_number %>

    <!--お支払い方法、プルダウン-->
    <p><%= f.label :"お支払い方法" %>
    <%= f.select :payment, [["現金", "現金"], ["クレジット", "クレジット"]] ,class:"form-control" %></p>
    <h4>※注意…店主はクレジットカードでの支払いを好まない為、カード情報の入力フォームは存在しません。<br>
    それでもクレジットカードでの購入を望む場合は長野にある店舗まで直接ご来店ください。</h4>


<% end %>


<!--カード決済、リンク先-->


<h3>購入商品詳細</h3>
<!--購入詳細テーブル-->
	<table class="table table-striped">
		<thead>
			<tr>
				<th>商品名</th>
				<th>金額</th>
				<th>数量</th>
			</tr>
		</thead>

		<tbody>
		<!--持ってくるのはproducts?,purchases?-->
		<% @cart_products.each do |cp| %>
			<tr>
				<!--商品名、金額、数量データの反映-->
				<td><%= cp.product.cd_name %></td>
				<td><%= cp.product.price %></td>
				<td><%= cp.quantity %></td>
			</tr>
		<% end %>
		</tbody>
	</table>


<!--購入確定、戻るボタン、リンク先-->
　　<div class="actions">
          <%= link_to '購入確定', top_path, class: "btn btn-success" %>
          <%= link_to '戻る', carts_path, class: "btn btn-info" %>
    </div>
