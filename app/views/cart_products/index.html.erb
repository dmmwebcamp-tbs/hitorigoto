<!-- カート内確認 -->
    <table class="table table-striped">

      <thead>
        <tr>
          <th>ジャケット画像</th>
          <th>ディスク名</th>
          <th>値段</th>
        </tr>
      </thead>

      <tbody>
       <% @cart_products.each do |cart| %>
        <tr>
          <!-- 商品情報 -->
          <!-- ジャケット画像 -->
          <td class="image">
          <%= attachment_image_tag cart.product, :image, :fill, 50, 50, fallback:"no_image.jpg" %>
          </td>
          <!-- CD名 -->
          <td><%=link_to cart.product.cd_name, product_path(cart) %></td>
          <!-- 値段 -->
          <td><%= cart.product.price %></td>

          <!-- 購入取消ボタン -->
          <td><%= link_to "購入取消", carts_path(@products), method: :delete, data: { confirm: '購入を取り消しますか?'} %></td>
        </tr>
        <% end %>
      </tbody>

    </table>


    <!-- 購入手続き、戻るボタン -->

        <div class="actions">
          <%= link_to '購入手続きに進む', account_path, class: "btn btn-success" %>
          <%= link_to '戻る', top_path, class: "btn btn-info" %>
        </div>


